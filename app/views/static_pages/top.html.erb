<div class="relative">
  <!-- モバイル向け画像 -->
  <%= image_tag 'Responsive.Top.png', alt: 'モバイル画像', class: 'block sm:hidden w-full h-auto' %>

  <!-- デスクトップ向け画像 -->
  <%= image_tag 'top.png', alt: 'デスクトップ画像', class: 'hidden sm:block w-full h-auto' %>

  <% if user_signed_in? %>
    <div class="bg-customblue text-customred font-bold py-8 px-4 rounded-full w-96 text-center text-4xl absolute top-[65%] left-[5rem]">
      <%= link_to "日記を書く".html_safe, new_journal_path(from: 'top') %>
    </div>
    <div class="bg-customblue text-customred font-bold py-8 px-4 rounded-full w-96 text-center text-4xl absolute top-[80%] left-[5rem]">
      <%= link_to "過去の日記一覧".html_safe, journals_path %>
    </div>
  <% else %>
    <div class="bg-customblue text-customred font-bold py-7 px-4 rounded-full w-96 text-center text-4xl absolute top-[65%] left-[5rem]">
      <p>日々の感情や思い出を音楽と一緒に<br>記録しませんか？</p>
    </div>
  <% end %>
</div>
