<header class="fixed top-0 left-0 right-0 bg-header shadow z-50">
  <div class="container mx-auto px-4 h-16 flex items-center justify-between">
    <%= render 'shared/header_logo' %>
    <%= render 'shared/header_nav' %>
  </div>
</header>

<div class="h-16"></div>