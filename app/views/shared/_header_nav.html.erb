<nav class="flex justify-around items-center space-x-8 text-lg">
  <%= link_to '人の日記を覗きにいく', '#', class: 'text-center text-gray-600 font-bold' %>
  <% if user_signed_in? %>
    <div class="flex justify-around items-center space-x-6">
         <%= link_to '過去の日記一覧', "#", class: 'text-center text-gray-600 font-bold' %>
        <%= link_to 'プロフィール', "#", class: 'text-gray-600 font-bold' %>
        <%= button_to 'サインアウト', "#", method: :delete, class: 'text-center text-gray-600 font-bold text-gray-400' %>
    </div>
  <% else %>
        <%= link_to 'サインイン', new_user_session_path, class: 'text-center text-gray-600 font-bold' %>
        <%= link_to 'サインアップ', new_user_registration_path, class: 'text-center text-gray-600 font-bold' %>
        <%= link_to 'パスワード再発行', new_user_password_path, class: 'text-center text-gray-600 font-bold' %>
  <% end %>
</nav>
